{
  "buildCommand": "env | grep -E '^VERCEL|^CI=|^PERCY' | sort; npx percy upload ./shots || true",
  "outputDirectory": "site",
  "installCommand": "npm ci --no-audit --no-fund",
  "framework": null
}
